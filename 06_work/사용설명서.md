# 🔐 암호화 유틸리티 사용설명서

웹 브라우저에서 실행되는 암호화 도구로, 텍스트·단일 파일·여러 파일을 AES‑GCM(256bit) 방식으로 암호화/복호화할 수 있습니다. `index.html`을 브라우저에서 열면 설치 없이 바로 사용할 수 있습니다.

## 1. 시작하기
- `index.html` 파일을 더블 클릭하거나 브라우저에서 열면 앱이 로드됩니다.
- 상단 탭(텍스트 암호화 / 파일 암호화 / 배치 처리) 중 원하는 모드를 선택하세요.
- 모든 모드에서 암호화 키는 **직접 입력**하거나 **키 생성** 버튼으로 안전한 키를 만들 수 있습니다.

## 2. 텍스트 암호화 탭
1. `텍스트 입력` 영역에 암호화할 문자열을 작성합니다.
2. `암호화 키` 입력 후 `암호화` 버튼을 누르면 결과가 오른쪽 `결과` 영역에 Base64 문자열로 표시됩니다.
3. 이미 암호화된 텍스트를 복호화하려면 `텍스트 입력`에 해당 값을 붙여 넣고 `복호화`를 선택합니다.
4. `초기화` 버튼으로 입력/출력 영역을 모두 비울 수 있으며, `결과 복사` 버튼으로 결과를 클립보드에 복사할 수 있습니다.
5. `👁️` 버튼은 키 입력 칸의 가시성을 토글합니다.

## 3. 파일 암호화 탭
1. `파일 선택` 영역에서 파일을 선택하거나 드래그합니다. 파일명/크기/타입 정보가 아래에 표시됩니다.
2. 암호화 키를 입력한 뒤 `암호화` 클릭 시 `<원본파일명>.encrypted` 파일이 다운로드됩니다.
3. 암호화된 파일을 복호화하려면 `파일 선택`에 해당 파일을 올리고 `복호화`를 누르세요. 원본 파일명이 자동으로 복원되어 다운로드됩니다.

## 4. 배치 처리 탭
1. 여러 파일을 동시에 올리면 목록과 용량 정보가 표시됩니다.
2. 키 입력 후 `전체 암호화` 또는 `전체 복호화`를 누르면 순차 처리되며, 진행률 바와 개별 결과(성공/오류 메시지 포함)를 확인할 수 있습니다.
3. `목록 초기화`는 선택한 파일 목록과 진행 기록을 모두 제거합니다.

## 5. 알림 및 보조 기능
- 상단 우측 알림 영역에서 작업 성공, 오류, 안내 메시지가 3초간 표시됩니다.
- 키 생성 버튼은 32글자 영숫자+특수문자 조합을 무작위로 생성합니다.
- 파일 크기는 KB/MB 단위로 자동 환산되어 표시됩니다.

## 6. 기술 정보 및 주의사항
- 암호화 방식: AES‑GCM 256bit, PBKDF2(SHA‑256, 100,000 iterations)로 키 스트레칭.
- 암호화된 결과물에는 salt(16바이트)와 IV(12바이트)가 포함되므로 동일한 키라도 안전하게 사용 가능합니다.
- 키는 서버에 저장되지 않으므로 **키 분실 시 복구 불가**합니다. 별도로 안전한 장소에 보관하세요.
- 브라우저 내 SubtleCrypto API를 사용하므로 최신 브라우저(Chrome/Edge/Firefox 등)에서 사용하세요.

## 7. 문제 해결
- `복호화 실패` 메시지가 뜨면 키가 다르거나 데이터가 손상된 경우입니다. 입력값과 키를 다시 확인하세요.
- 파일 업로드가 되지 않으면 브라우저 권한 또는 파일 크기 제한(대용량 파일은 처리 시간이 길 수 있음)을 확인하세요.
- 기타 문제는 `confidential.txt` 혹은 관련 문서의 지침을 먼저 확인한 후 개발자에게 문의하세요.

필요 시 UI 텍스트와 버튼 레이블은 `index.html`, 동작 로직은 `script.js`, 스타일은 `style.css`에서 수정할 수 있습니다.
